we're testing out a provider that we're building, we can't rely on how our provider is setup. We need to consult the documentation in the openai-go directory and the sdkv2 directory, those are our sources of documentation.

All documentation needs to follow same naming convention, ending with the .md extension.